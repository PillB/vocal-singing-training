# Value Gap Registry (living)

Shared registry updated after each research/implementation phase.  
Product: **Vocal Studio / PillB**.

| ID | Gap | Severity | Status | Owner / next | Evidence |
|----|-----|----------|--------|--------------|----------|
| VG-01 | Stripe/MP payment links empty; demo unlock on | P0 | **Mitigated** | Demo unlock off + checkout held until verification is live; Pablo still to paste live links | billing-config.js |
| VG-02 | Pro marketed multi_profile / priority_progressions undelivered | P0 | **Mitigated** | Honest feature list; coach .txt export | Hormozi likelihood; packaging fix 2026-07-16 |
| VG-03 | Pro uniqueness thin vs free | P1 | Open | Multi-profile + insights v2 | Free-rich freemium |
| VG-04 | Multi-profile flag only | P1 | **Closed** | Local multi-profile (≤3 Pro) | Shipped 2026-07-16 |
| VG-05 | Insights aggregate text only | P1 | **Closed** | Insights v2 sparkline + coach focus | Shipped 2026-07-16 |
| VG-06 | Trial auto-starts for all browsers | P2 | **Closed** | Opt-in via pricing CTA (`trialRequiresOptIn`) | Shipped 2026-09-19 |
| VG-07 | 12-week plan weakly coupled to completions | P2 | Open | Plan→exercise deep links | Habit likelihood |
| VG-08 | Entitlement local-only / shareable | P2 | **Closed** | `workers/entitlements/` + ES256 license verified in `js/license.js` | Shipped 2026-09-19 |
| VG-09 | Many vocal modes self-report | P2 | Open | More mic-derived detectors | Expectation vs delivery |
| VG-10 | No social proof on home | P2 | Open | Outcome quotes strip | Likelihood of achievement |
| VG-11 | No competitor differential on pricing | P2 | Open | Free forever vs time-cap matrix | Reddit Yousician |
| VG-12 | No product analytics | P1 | Open | Plausible/GA4 events | Funnel optimization |
| VG-13 | Home hero is track name not dream outcome | P2 | Open | Outcome line i18n | Hormozi dream outcome |
| VG-14 | Coach pack was name-only | P1 | **Closed** | JSON + .txt dual download | Fixer 2026-07-16 |
| VG-15 | No passive free-tier monetization beyond Pro | P2 | **Strategy + scaffold** | Native ads off-by-default; privacy + ad-free Pro framing; see `19-AD-…` | Validated 2026-07-16 |
| VG-16 | No accounts: progress lived and died in one browser | P0 | **Closed (code)** | Email-code + Google sign-in, D1-backed; Pablo still to create the D1 database and set the keys | Shipped 2026-09-21, `docs/33-…` |
| VG-17 | No way to gift a free month, or take one back | P1 | **Closed (code)** | Trial/gift/comp are one grant row with `revoked_at`; codes and direct grants; admin panel | Shipped 2026-09-21, `docs/33-…` |
| VG-18 | Trial was per-browser, so it could be farmed by clearing storage | P1 | **Closed (code)** | Once accounts are configured the trial is one per account, ever (`trial_used_at`) | Shipped 2026-09-21 |
| VG-19 | Peru cannot be a Stripe merchant, so "international payments" had no rail | P0 | **Decided, not executed** | Mercado Pago Perú + a merchant of record (Creem first, Polar in parallel); Pablo to register | `docs/34-PERU-OPERATOR-RUNBOOK.md` |
| VG-20 | The client can only check out through Stripe or Mercado Pago, but the chosen international rail is a merchant of record | P0 | Open | `PROVIDER_IDS`, the checkout host allowlist, `preferredRail()` and every `markets[].rail` still say `stripe`, and the worker's claim route rejects any other provider. Pasting a Creem link into the config would be refused by our own validation. Needs a Creem rail end to end: host allowlist, provider id, webhook handler and signature check | `js/billing.js:27,33,108`, `workers/entitlements/src/index.js:259` |
| VG-21 | The yearly badge asserted a discount the prices did not give | P2 | **Closed** | Said "Save 20%" against a real 34%. Derived from the prices now, with two tests | Shipped 2026-09-22, `js/billing.js` `annualSavingPct` |
| VG-22 | Prices were set without reference to any market: Peru was charged more than Yousician and Duolingo charge Peruvians | P0 | **Closed** | S/ 35 → S/ 19.90, S/ 279 → S/ 119.90, $9.99 → $7.99, $79 → $49. `pricePen` was a spot conversion of `priceUsd`; the measured Peru benchmark is ~70% of the US price, ~59% after IGV | Shipped 2026-09-22, `docs/35-PRICING.md` |
| VG-23 | Pricing copy promised Stripe as the processor and claimed a year costs "less than 1 private lesson" | P1 | **Closed** | Stripe cannot be the seller from Peru, so the rail label and the checkout note no longer name it. A Lima voice lesson averages S/ 45/h, so a year at S/ 279 was six of them; the claim now says "less than 3", which is true in both markets | Shipped 2026-09-22 |
| VG-24 | The tour described a home page that no longer exists, and covered the elements it pointed at | P1 | **Closed** | One of twelve steps pointed at `.continue-toolbar`, deleted in the September redesign, so the counter read "Paso 1 de 11" and the guided session was never mentioned. The popover sat at 100% coverage of its own target on the play-mode step, the backdrop dimmed the element it was highlighting, and `body.tour-active { overflow: hidden }` made every `scrollIntoView` in the tour a no-op. Rebuilt to four steps with searched placement, a real spotlight hole, and a phone sheet | Shipped 2026-09-22, `docs/36-TOUR-AND-USER-GUIDE.md` |
| VG-25 | The site had no written documentation of any kind | P1 | **Closed** | Nothing explained the pitch highway's colours, the ±35-cent lane height, the 65–400 Hz detector range, which exercises need the microphone, or what a denied microphone looks like. `guide.html`: 14 sections, Spanish and English, static, linked from every footer and from each tour step | Shipped 2026-09-22, `guide.html` |
| VG-26 | The microphone was requested with no warning, and a denial looks like success | P2 | **Closed** | On a pitch exercise a denied microphone still plays the piano and scrolls the highway; only the voice line is missing, and the denial is then only undoable in browser settings. `#mic-primer` explains the prompt before the browser shows it, once per browser | Shipped 2026-09-22, `js/tour.js` |
| VG-27 | The guided-session Next button advanced without saving anything | P1 | **Closed** | Practice is now recorded by `recordPracticeIfDue()` on every exit — Next, Skip, leaving, switching, the tab hiding or closing, the timer running out — once the step has run max(15 s, 30%) capped at 45 s. One take per open, replaced in place by a longer record or a Save, so nothing is counted twice | Fixed 2026-09-23, `js/app.js`, `docs/37-DAILY-LOOP.md` |
| VG-28 | Leaving with "Save" pointed the user at a button inside a collapsed card | P2 | **Closed** | The save path now opens the metrics card before focusing its button. The button also reads "Puntuar ahora" instead of "Guardar progreso", since the practice is kept either way | Fixed 2026-09-23, `js/app.js` `leaveExercise()` |
| VG-29 | The post-practice score screen is English in the Spanish UI | P2 | Open | `VTMetrics.score()` hard-codes every string it returns: all four `summary` bands, the `how` explanation, `"Not rated"`, `"Not logged"`, `"(lower is better)"` and `"target"`. A Spanish learner finishes a run and the entire explanation of their score is in English | `js/metrics.js:24,43,49,60,64,91` |
| VG-30 | On a rotated phone the practice stage can measure past the bottom of the screen, putting Start out of reach | P2 | Open | At 844x390 the stage bottom measures ~400px against a 390px viewport. Intermittent and load-dependent: green in 10 isolated runs, but **8 of 15 repeats fail under 4-worker contention on this branch and 5 of 15 on `main`**, so it is pre-existing, not a regression (Fisher exact p≈0.46 between the two). Suspected cause is the stage height clamp recomputing from the page top while the stage top is scrolled above the viewport; the fix is to shrink by the measured overflow | `tests/viewport-overflow.spec.js:125`, `fitHighwayToViewport()` |
| VG-31 | Tapping the thing the tour is pointing at closed the tour, and took the coach-marks with it | P1 | **Closed** | `.tour-backdrop` covers the spotlight hole and `.tour-highlight` carries `pointer-events: none !important`, so the lit element could not receive a click — it landed on the backdrop, which called `end("skip")` and wrote `dismissed`. That flag also suppresses every per-screen coach-mark, so one stray tap silently declined all of the site's onboarding, with no way back except a header button the tour had not reached yet. On a 390px phone the dim is about 64% of the screen. Now a click inside the ring advances the step and a click on the dim does nothing | Fixed 2026-09-22, `js/tour.js` `inSpotlight()` |
| VG-32 | Every link into the user guide landed with the section title hidden under the sticky header | P2 | **Closed** | `.app-header` is sticky at ~74px and no heading had `scroll-margin-top`, so all 40 contents links, every inline cross-reference and all four of the tour's "full guide" links arrived mid-paragraph: `document.elementFromPoint()` at the heading's position returned `.app-header`. Both languages | Fixed 2026-09-22, `guide.html` |
| VG-33 | The microphone primer promised a piano and a pitch readout on exercises that have neither | P2 | **Closed** | One string was shown for all 45 microphone exercises: "the piano keeps playing, only the pitch readout is lost". That is false for 23 of them — including `v1-diction`, which is what the home page's own first-practice button opens — so the first thing the product says to a new user was wrong about the screen they were looking at. Split into a with-piano and a without-piano wording, chosen by `exerciseWantsSound()` | Fixed 2026-09-22, `js/i18n.js` `tour.mic.bodyPiano` |
| VG-34 | Answering "Ahora no" to the primer silently cancelled Start, then let the next press through unexplained | P2 | **Closed** | Declining closed the dialog and returned, abandoning the `startPractice()` the user had just pressed, with nothing said. It also set `vt_mic_primed_v1`, so the second press went straight to the browser prompt — the primary button of the product did nothing the first time and something different the second. Declining now says so | Fixed 2026-09-22, `js/app.js`, `tour.mic.declined` |
| VG-35 | Switching to English left the tour invitation in Spanish, pointing at the Spanish guide | P2 | **Closed** | `renderTourInvite()` returned early if the row already existed, so its four phrases kept whatever language the page first loaded in — and the site defaults to Spanish, so every English speaker saw them in Spanish. `guide.html` holds both languages in one file, so its bare links also always landed an English reader on the Spanish half. The row is rebuilt on every render and every `guide.html` link is rewritten per language | Fixed 2026-09-22, `js/app.js` `syncGuideLinks()` |
| VG-36 | Nine controls were labelled in Spanish for screen readers whatever the language | P3 | **Closed** | The octave group and its two buttons, the microphone sensitivity slider, the auto-range checkbox, the pitch canvas, the tier filter group and the account and tour buttons carried hard-coded `aria-label`/`title` text in `index.html`. A blind English user heard "Bajar una octava" | Fixed 2026-09-22, `index.html`, `js/i18n.js` |
| VG-37 | The last tour step sent readers to the same guide section as the first | P3 | **Closed** | Step 4 describes the header — Plan, History, Pro, Account — and its "full guide" link pointed at `#que-es`, the introduction step 1 already links to. Now `#guardar`, which is the section about saving and history | Fixed 2026-09-22, `js/tour.js` |
| VG-38 | The tour dropped you at the top of the home page wherever you started it | P3 | **Closed** | `end()` called `goHome()` unconditionally, so replaying the tour from halfway down the catalog cost you your place. It now records the view and scroll position it found and restores both. `html { scroll-behavior: smooth }` makes an `"auto"` restore a one-second glide across the whole page, so the restore is `"instant"`, and it runs twice a frame apart because the home page renders its lower sections lazily and the browser otherwise clamps the scroll to a page that is still short | Fixed 2026-09-22, `js/tour.js` `restorePlace()` |
| VG-39 | An auto-started tour reopened on every reload | P3 | **Closed** | Under `?ab_tour_shape_2026_10=auto` the 600ms timer fired on every load until the tour was finished or explicitly dismissed, so a visitor who simply reloaded past it got it again, and again. A forced `?ab_` view also spent the browser's one recorded exposure, dropping that visitor out of the experiment for good and leaving the stored exposure disagreeing with the arm they are served afterwards | Fixed 2026-09-22, `js/tour.js`, `js/experiments.js` |
| VG-40 | Saved sessions were English in the Spanish UI, and a denied microphone froze a guided session | P2 | **Closed** | The step clock no longer waits for the microphone, and a refusal shows a notice on the stage with how to allow it, a retry and, inside a routine, Skip. The history list's buttons, its "last score" line and "Saved reviews" are translated, dates follow the interface language, and the home title no longer reads "sesión(es)". The score screen's own strings are still VG-29 | Fixed 2026-09-23, `js/app.js` `showMicBlocked()`, `js/i18n.js` |
| VG-41 | Nothing to come back to: the catalog and the 12-week plan are an arc, and people walked through them and left | P0 | **Closed (code)** | The daily loop: today's basics in three sizes with an identical 3-minute Mínimo, a lifetime days-sung count, a weekly range goal, rest days, a kind comeback, sparse informational surprises and a "done for today" state. Whether it moves return rates cannot be measured until VG-12 has an endpoint | Shipped 2026-09-23, `js/daily-loop.js`, `docs/37-DAILY-LOOP.md` |
| VG-42 | Practice days were UTC and the streak was capped | P1 | **Closed** | In Lima the day turned over at 19:00, so an evening singer lost streaks they had earned; the streak was rebuilt from history capped at 50 takes per exercise. Now a local, uncapped, synced ledger (`vt_days_v1`) | Fixed 2026-09-23, `js/practice-days.js` |
| VG-43 | The streak freeze never protected a streak | P1 | **Closed** | It spent itself the evening after any practice and the streak never read it. Replaced by rest days: earned by practising, spent only on a day really missed and only when they cover the whole gap | Fixed 2026-09-23, `js/practice-days.js`, `js/reminders.js` |
| VG-44 | The primary buttons failed WCAG contrast | P2 | **Closed** | White on the green practice button read 2.5:1, on the Canto orange 2.6:1, on the blue end of the primary gradient 3.5:1; the active Vocal and Canto track tabs read 3.2:1 and 2.9:1. Dark labels on the green and orange, darker blue ends | Fixed 2026-09-23, `css/styles.css` |
| VG-45 | A night singer's 00:30 practice counts for the next day | P3 | Open | The research recommends a late rollover (about 3 a.m.). The heatmap and plan code pass midnight dates to the day key, so the shift has to change those callers together | `js/practice-days.js` `dayKey()`, `js/value-pulse.js` |
| VG-46 | The live pitch trace may teach dependence on itself | P2 | Open | Concurrent visual feedback improves accuracy while it is on screen; carry-over without it is mixed. "Sing first, then see" reps, with the trace hidden and shown afterwards, are the standard remedy and need a practice-screen change | Research write-up, motor learning |
| VG-47 | Every screen is light text on dark | P3 | Open | Dark-on-light reads better for text-heavy screens (guide, plan, cards) at every age; the dark stage is still right for the highway. A light theme that follows the system setting | Research write-up, colour |

## Change log
| Date | Change |
|------|--------|
| 2026-07-16 | Registry created from orchestration Phases 0–8 |
| 2026-07-16 | VG-02 mitigated; VG-14 closed; coach .txt export |
| 2026-07-16 | VG-15 ad strategy + scaffold (adsEnabled false) |
| 2026-09-19 | VG-08 closed: entitlements worker (Stripe + MP webhooks, KV, ES256 licenses) and browser-side verification |
| 2026-09-19 | VG-06 closed: free trial is opt-in; no browser is silently Pro |
| 2026-09-19 | VG-01 mitigated: `demoUnlockEnabled:false`, checkout refuses to run until verification is configured. Remaining: live payment links + worker deploy (operator) |
| 2026-09-21 | VG-16/17/18 closed in code: accounts, saved progress across devices, and one grant mechanism for trial, gift and comp. All optional — with no D1 binding the worker behaves exactly as before |
| 2026-09-21 | VG-19 opened: Peru is not a Stripe merchant country, so Stripe cannot be the seller. Direction is Mercado Pago Perú plus a merchant of record for the rest of the world |
| 2026-09-21 | VG-19 corrected: the first reading — that Polar and Lemon Squeezy inherit Stripe's restriction — was wrong. Stripe added Peru as a cross-border *payout* destination on 2026-02-25, and Polar, Creem, Lemon Squeezy and Gumroad each name Peru on their own supported-country pages. Creem is cheapest at this price point (3.9% + $0.40); Lemon Squeezy is ruled out for being folded into Stripe Managed Payments, which does not cover Peru |
| 2026-09-22 | VG-22 opened and closed: prices re-derived from evidence. Seven apps measured across paired Peru and US App Store listings put the Peru benchmark at 69.7% of the US price (59% after backing out IGV), against our ~100%. Yousician charges Peruvians S/ 30.90/month and Duolingo S/ 32.90 while we asked S/ 35. Separately the Education category's median annual is $38-45 against our $79, while our $9.99 monthly was already at the median — so the dollar problem was the annual, not the monthly. New: S/ 19.90 / S/ 119.90 and $7.99 / $49, a 50% and 49% annual discount |
| 2026-09-22 | VG-19 reinforced from practitioner evidence (forums, articles, testimonials, fact-checked): Peruvian issuers approve roughly 70–90% of locally acquired charges against 30–50% acquired abroad, which decides Mercado Pago-first on transactions rather than on fees; Culqi demoted from fallback to last resort (its own subscription tooling is unmaintained — 12 open issues, the subscription request unanswered 5.5 years — and merchants rate it worst on fraud), Openpay BBVA becomes the fallback; Peru price moved from S/ 25–30 to S/ 20–25 against the streaming anchor; a Cloudflare billing alert added to stage 1 |
| 2026-09-22 | VG-24/25/26 closed: the tour rebuilt against the home page the site actually has, a written bilingual user guide added at `guide.html`, and a microphone primer added before the browser's permission prompt. `js/experiments.js` ships the variant assignment the tour A/B needs, disabled — a 5-point effect needs ~1,570 browsers per arm, which this site does not have, so the machinery ships dormant rather than producing a number that looks like evidence |
| 2026-09-22 | VG-27/28/29 opened, found while walking the flows for the guide and deliberately not fixed in a documentation change: a guided session that saves nothing, a Save path that focuses an invisible button, and an English score screen in the Spanish UI. VG-27 is the serious one — a learner completing real work and being told nothing was recorded |
| 2026-09-22 | VG-30 opened: the phone-landscape stage overflow, previously known but never filed, measured on both this branch and `main` under contention so the intermittency is on the record rather than in someone's memory |
| 2026-09-22 | VG-31 to VG-39 opened and closed in one pass: 8 flows driven in a real Chromium with a genuine Chrome user agent, each finding re-reproduced independently before it was believed. 29 raised, 28 confirmed, 1 refuted. VG-31 is the one that mattered — the tour's own spotlight was a trap that silently declined the whole of the site's onboarding. VG-40 opened for the two defects in the same pass that belong to `js/app.js` rather than to the tour |
| 2026-09-23 | VG-27, VG-28 and VG-40 closed and VG-41 to VG-44 opened and closed: the daily loop, built on a practice ledger that no longer loses practice. VG-41 is the product change the rest serves — a routine that is never finished, so there is always a reason to come back. VG-45 to VG-47 opened from the research for later |
