/**
 * Exercise pictures — resonance. Registers painters on window.VTViz.scenes.
 * (Filled in by the resonance build; see js/scenes/speech.js for the pattern.)
 */
(function (global) {
  "use strict";
  const V = global.VTViz;
  if (!V) return;
})(typeof window !== "undefined" ? window : globalThis);
