/**
 * Exercise pictures — speech-shape. Registers painters on window.VTViz.scenes.
 * (Filled in by the speech-shape build; see js/scenes/speech.js for the pattern.)
 */
(function (global) {
  "use strict";
  const V = global.VTViz;
  if (!V) return;
})(typeof window !== "undefined" ? window : globalThis);
